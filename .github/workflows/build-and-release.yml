name: Build and Release

on:
  push:
    tags:
      - 'v*'
  workflow_dispatch:

permissions:
  contents: write

jobs:
  build-mac-arm64:
    runs-on: macos-latest
    steps:
      - name: Checkout code
        uses: actions/checkout@v4

      - name: Setup Node.js
        uses: actions/setup-node@v4
        with:
          node-version: '20'
          cache: 'npm'

      - name: Install dependencies
        run: npm ci

      - name: Clean previous builds
        shell: bash
        run: |
          rm -rf release/ dist/ dist-electron/ build/
          mkdir -p build

      - name: Prepare icons
        shell: bash
        run: |
          if [ ! -f "build/icon.ico" ] || [ ! -f "build/icon.icns" ] || [ ! -f "build/icon.png" ]; then
            npm run prepare-icons
          else
            echo "✅ Icon files already exist, skipping preparation"
          fi

      - name: Build TypeScript
        run: npx tsc

      - name: Build Vite
        run: npx vite build

      - name: Build for macOS ARM64
        run: npx electron-builder --mac --arm64 --publish never
        env:
          ELECTRON_BUILDER_ALLOW_UNRESOLVED_DEPENDENCIES: true

      - name: Upload artifacts
        uses: actions/upload-artifact@v4
        with:
          name: mac-arm64
          path: release/
          retention-days: 7
          if-no-files-found: warn

  build-mac-x64:
    runs-on: macos-latest
    steps:
      - name: Checkout code
        uses: actions/checkout@v4

      - name: Setup Node.js
        uses: actions/setup-node@v4
        with:
          node-version: '20'
          cache: 'npm'

      - name: Install dependencies
        run: npm ci

      - name: Clean previous builds
        shell: bash
        run: |
          rm -rf release/ dist/ dist-electron/ build/
          mkdir -p build

      - name: Prepare icons
        shell: bash
        run: |
          if [ ! -f "build/icon.ico" ] || [ ! -f "build/icon.icns" ] || [ ! -f "build/icon.png" ]; then
            npm run prepare-icons
          else
            echo "✅ Icon files already exist, skipping preparation"
          fi

      - name: Build TypeScript
        run: npx tsc

      - name: Build Vite
        run: npx vite build

      - name: Build for macOS x64
        run: npx electron-builder --mac --x64 --publish never
        env:
          ELECTRON_BUILDER_ALLOW_UNRESOLVED_DEPENDENCIES: true

      - name: Upload artifacts
        uses: actions/upload-artifact@v4
        with:
          name: mac-x64
          path: release/
          retention-days: 7
          if-no-files-found: warn

  build:
    strategy:
      matrix:
        include:
          - os: windows-latest
            platform: win
            arch: x64
          - os: ubuntu-latest
            platform: linux
            arch: x64

    runs-on: ${{ matrix.os }}

    steps:
      - name: Checkout code
        uses: actions/checkout@v4

      - name: Setup Node.js
        uses: actions/setup-node@v4
        with:
          node-version: '20'
          cache: 'npm'

      - name: Install dependencies
        run: npm ci

      - name: Clean previous builds
        shell: bash
        run: |
          rm -rf release/ dist/ dist-electron/ build/
          mkdir -p build

      - name: Prepare icons
        shell: bash
        run: |
          if [ ! -f "build/icon.ico" ] || [ ! -f "build/icon.icns" ] || [ ! -f "build/icon.png" ]; then
            npm run prepare-icons
          else
            echo "✅ Icon files already exist, skipping preparation"
          fi

      - name: Build TypeScript
        run: npx tsc

      - name: Build Vite
        run: npx vite build

      - name: Build for platform
        shell: bash
        run: |
          if [ "${{ matrix.platform }}" == "mac" ]; then
            npx electron-builder --mac --${{ matrix.arch }} --publish never
          elif [ "${{ matrix.platform }}" == "win" ]; then
            npx electron-builder --win --x64 --publish never
          else
            npx electron-builder --linux --x64 --publish never
          fi
        env:
          ELECTRON_BUILDER_ALLOW_UNRESOLVED_DEPENDENCIES: true

      - name: Upload artifacts
        uses: actions/upload-artifact@v4
        with:
          name: ${{ matrix.platform }}-${{ matrix.arch }}
          path: release/
          retention-days: 7
          if-no-files-found: warn

  release:
    needs: [build-mac-arm64, build-mac-x64, build]
    runs-on: ubuntu-latest
    if: startsWith(github.ref, 'refs/tags/v')

    steps:
      - name: Checkout code
        uses: actions/checkout@v4

      - name: Download all artifacts
        uses: actions/download-artifact@v4
        with:
          path: artifacts
          pattern: '*'
          merge-multiple: false

      - name: Get version from tag
        id: get_version
        run: echo "VERSION=${GITHUB_REF#refs/tags/v}" >> $GITHUB_OUTPUT

      - name: Prepare release files
        shell: bash
        run: |
          mkdir -p release-files
          
          # macOS files
          find artifacts/mac-arm64 -name "*.dmg" -o -name "*.zip" | xargs -I {} cp {} release-files/ 2>/dev/null || true
          find artifacts/mac-x64 -name "*.dmg" -o -name "*.zip" | xargs -I {} cp {} release-files/ 2>/dev/null || true
          
          # Windows files
          find artifacts/win-x64 -name "*.exe" | xargs -I {} cp {} release-files/ 2>/dev/null || true
          
          # Linux files
          find artifacts/linux-x64 -name "*.AppImage" -o -name "*.deb" | xargs -I {} cp {} release-files/ 2>/dev/null || true
          
          echo "Release files prepared:"
          ls -lh release-files/

      - name: Create Release
        uses: softprops/action-gh-release@v1
        with:
          tag_name: ${{ github.ref }}
          name: BeeLocal ${{ steps.get_version.outputs.VERSION }}
          body: |
            ## 🐝 BeeLocal ${{ steps.get_version.outputs.VERSION }}

            ### 📦 İndirme Dosyaları

            **macOS:**
            - BeeLocal-${{ steps.get_version.outputs.VERSION }}-arm64.dmg (Apple Silicon)
            - BeeLocal-${{ steps.get_version.outputs.VERSION }}-x64.dmg (Intel)

            **Windows:**
            - BeeLocal-Setup-${{ steps.get_version.outputs.VERSION }}-win-x64.exe (Kurulum)
            - BeeLocal-Portable-${{ steps.get_version.outputs.VERSION }}-win-x64.exe (Taşınabilir)

            **Linux:**
            - BeeLocal-${{ steps.get_version.outputs.VERSION }}-linux-x64.AppImage
            - BeeLocal-${{ steps.get_version.outputs.VERSION }}-linux-x64.deb

            ### 📝 Değişiklikler

            Detaylar için [CHANGELOG.md](https://github.com/${{ github.repository }}/blob/main/CHANGELOG.md) dosyasına bakın.

            ---
            Made with ❤️ by İTÜ Students
          files: release-files/*
          draft: false
          prerelease: false
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}

